import React from "react";
import { startTimer } from "../main";

const Continue = () => {
    return (
        <button onClick={startTimer}>
            Continuar
        </button>
    );
};

export default Continue;
